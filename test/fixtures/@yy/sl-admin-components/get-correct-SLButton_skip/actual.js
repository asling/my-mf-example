import { SLSelect, SLButton } from '@yy/sl-admin-components';
import React from 'react';

function App() {
  return React.createElement('div', {
    children: [
      React.createElement(SLButton),
      React.createElement(SLSelect),
    ]
  });
}
